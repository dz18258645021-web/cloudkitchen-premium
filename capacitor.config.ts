import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cloudkitchen.premium',
  appName: 'CloudKitchen Premium',
  webDir: 'dist'
};

export default config;
